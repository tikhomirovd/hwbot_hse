# CONTEXT

Telegram-бот сбора ДЗ для 1 курса БАЦРФ (группы БАЦРФ261 и БАЦРФ262).
Это не LMS: регистрация по ведомости, рассылка ДЗ, приём ссылок, строгий дедлайн, выгрузка.

## Где крутится

- Код: `/root/hwbot`
- Процесс: `systemd` юнит `hwbot.service`
- Токен и админы: `/root/hwbot/.env` (не коммитить, не логировать)
- SQLite: `/root/hwbot/data/bot.db`
- Список студентов: `/root/hwbot/data/roster.csv` (ФИО, группа, почта; без дат рождения)

Рестарт:

```bash
systemctl restart hwbot
systemctl status hwbot
journalctl -u hwbot -f
```

## Продукт

- Дедлайн строгий: после срока сдать нельзя, не сдал = 0.
- Постоянной reply-клавиатуры нет. Команды + inline «это ты?» / выбор ДЗ.
- Две группы: `261` / `262` в CLI, в базе `БАЦРФ261` / `БАЦРФ262`.
- Напоминания за 24ч и 12ч тем, кто ещё ничего не отправил и уже написал боту.
- Преподаватель обычно не жмёт кнопки в Telegram: просит в Cursor «сделай рассылку» / «выгрузи».

Админ Telegram id: `206078247`.

## Команды студентов

- `/start` — регистрация (ФИО или `@edu.hse.ru`) или профиль
- `/hw` — активные ДЗ и статус
- `/submit` — сдать / пересдать до дедлайна
- `/mysubmissions` — свои сдачи
- `/help`
- `/cancel`

## Команды админа

- `/newhw` — создать ДЗ диалогом
- `/status 1` — кто сдал
- `/missing 1` — кто не сдал
- `/export 1` — CSV

## Как делать рассылку и выгрузку из нового чата

Сначала прочитай этот файл. Потом из `/root/hwbot`:

```bash
uv run hwbot create-hw \
  --title "ДЗ 1. Git" \
  --text "Сдайте ссылку на репозиторий" \
  --deadline "2026-09-12 23:59" \
  --groups 261,262

uv run hwbot status --hw 1
uv run hwbot export --hw 1 --out /tmp/hw-1.csv
uv run hwbot list-hw
```

`--no-broadcast` создаёт ДЗ без рассылки (для отладки).

Дедлайн в `Europe/Moscow`.

## Чего не делать

- Не логировать токен, сдачи, ФИО и почту без нужды
- Не класть в репозиторий и БД даты рождения
- Не принимать сдачи после дедлайна и не «смягчать» 0
- Не добавлять постоянную клавиатуру
- Не превращать пакет в RAG/агента/общий GigaChat SDK
- Не коммитить `.env` и `data/bot.db`

## Стек

Python 3.12, `uv`, aiogram 3, aiosqlite, rapidfuzz.
Тесты без сети: `uv run pytest`
Типы: `uv run mypy src` и `uv run pyright`
